---
description: Run an independent evaluator sub-agent against the current artifact (UI / code / writing / i18n / custom) using a configured rubric. Returns Accept / Revise / Block with evidence.
---

Read `~/.claude/skills/harness/SKILL.md`, then `~/.claude/skills/harness/modes/evaluate.md`. Follow both.

ARGUMENTS: required — evaluator kind (`ui` / `code` / `writing` / `i18n` / custom name from `.harness/config.json`'s `evaluators` map).
