---
description: Bootstrap harness engineering for the current project — detect stack, scaffold .harness/, integrate with CLAUDE.md. One-time per project.
---

Read `~/.claude/skills/harness/SKILL.md` (shared rules + startup checks), then read `~/.claude/skills/harness/modes/bootstrap.md` (this mode's procedure). Follow both.

ARGUMENTS: optional — `--minimal` to skip ui detection, generate only the base 4 files.
