---
description: Write a session handoff to .harness/progress.md so the next session can resume cleanly. Also fired automatically by the Stop hook with --auto.
---

Read `~/.claude/skills/harness/SKILL.md`, then `~/.claude/skills/harness/modes/handoff.md`. Follow both.

ARGUMENTS: optional — `--auto` (skip the "session goal" extraction step; used by Stop hook).
